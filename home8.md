

---

# 🛡️ إجابات تمرين التفكير التطبيقي (سيناريو أمني)

يستعرض هذا المستند تحليلاً أمنياً شاملاً لكيفية تأمين أنظمة تسجيل الدخول وحماية البيانات من الهجمات الشائعة، مع التركيز على أفضل الممارسات البرمجية.

---

### 1. تحديد الخطر الأمني الأكبر 🚨

الخطر الأكبر يحدث عند تأليف استعلام SQL باستخدام مدخلات المستخدم مباشرة دون معالجتها، مما يؤدي إلى ثغرة **حقن SQL (SQL Injection)**.

**❌ مثال على كود مصاب:**

PHP

```
$email = $_POST['email'];
$password = $_POST['password'];
// خطر جداً: دمج المتغيرات مباشرة في النص
$sql = "SELECT * FROM users WHERE email = '$email' AND password = '$password'";
$result = mysqli_query($conn, $sql);
```

---

### 2. لماذا نستخدم `prepare()` بدلاً من `query()`؟ 🏗️

حتى مع استخدام دالة التطهير اليدوي مثل `mysqli_real_escape_string` مع `query()`، فإنها تظل غير كافية أحياناً.

- **الفرق الجوهري:**
    
    - **`prepare()` + `bind_param()`:** تفصل بين "الشفرة البرمجية" و "البيانات" تماماً، حيث يتم إرسال الاستعلام للهيكل أولاً ثم البيانات لاحقاً، فلا يمكن تنفيذ البيانات ككود.
        
    - **`query()`:** تدمج البيانات مع الشفرة في نص واحد، مما يترك مجالاً للتلاعب.
        

---

### 3. سياسة رسائل الخطأ الآمنة 📢

يجب منع المهاجم من معرفة سبب فشل تسجيل الدخول بدقة لمنع عملية "حصر الحسابات" (Account Enumeration).

- **❌ خطأ (يكشف معلومات):** "البريد الإلكتروني غير مسجل في النظام".
    
- **✅ صحيح (رسالة عامة):**
    

PHP

```
echo "البريد الإلكتروني أو كلمة المرور غير صحيحة";
```

---

### 4. التحقق من قوة كلمة المرور 🔐

لا يكفي تشفير كلمة المرور، بل يجب التأكد من أنها قوية منذ البداية.

PHP

```
if (strlen($password) < 8) {
    die("كلمة المرور يجب أن تكون 8 أحرف على الأقل");
}
```

---

### 5. الحماية من هجمات القوة الغاشمة (Brute Force) 🔨

الهجوم المتكرر لتخمين كلمات المرور يتطلب حماية تعتمد على الوقت وعدد المحاولات.

**💡 كود الحماية المطبق:**

PHP

```
session_start();
if (!isset($_SESSION['login_attempts'])) {
    $_SESSION['login_attempts'] = 0;
    $_SESSION['attempt_time'] = time();
}

// إذا تجاوز 5 محاولات في أقل من 15 دقيقة
if ($_SESSION['login_attempts'] > 5 && (time() - $_SESSION['attempt_time']) < 900) {
    die("لقد تجاوزت عدد المحاولات المسموح بها. حاول بعد 15 دقيقة");
}
```

---

### 6. استخدام HTTPS ورموز CSRF Tokens 🕵️‍♂️

يجب حماية النماذج من التزوير عبر المواقع (CSRF) لضمان أن الطلب قادم من المستخدم الفعلي عبر موقعنا.

**نموذج HTML آمن:**

HTML

```
<form method="POST">
    <input type="hidden" name="csrf_token" value="<?php echo $_SESSION['csrf_token']; ?>">
    <input type="email" name="email">
    <input type="password" name="password">
    <button type="submit">تسجيل الدخول</button>
</form>
```

---

### 7. سجلات المراقبة (Security Logs) 🔍

النظام الاحترافي يجب أن يسجل الأحداث الأمنية للرجوع إليها عند وقوع اختراق.

**الأحداث الواجب تسجيلها:**

- محاولات الدخول الفاشلة (مع تسجيل الـ IP).
    
- تغيير كلمات المرور.
    
- تغيير البريد الإلكتروني أو الصلاحيات.
    

---

### 📊 ملخص مصفوفة الحماية الأمنية

|**الإجراء الأمني**|**التهديد الذي يعالجه**|**الأهمية**|
|---|---|---|
|**Prepared Statements**|SQL Injection|🔴 حرجة جداً|
|**Generic Error Messages**|Account Enumeration|🟡 متوسطة|
|**Rate Limiting**|Brute Force Attacks|🟠 عالية|
|**CSRF Tokens**|Cross-Site Request Forgery|🟠 عالية|

---
